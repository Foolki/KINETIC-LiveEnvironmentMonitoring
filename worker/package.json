{
    "name": "kinetic-worker",
    "version": "1.0.0",
    "description": "Background Integration Worker for Kinetic",
    "main": "dist/worker.js",
    "scripts": {
        "build": "tsc",
        "start": "node dist/worker.js",
        "dev": "ts-node-dev --respawn --transpile-only src/worker.ts"
    },
    "dependencies": {
        "bullmq": "^5.0.0",
        "dotenv": "^16.0.0",
        "ioredis": "^5.3.0",
        "mongoose": "^8.0.0"
    },
    "devDependencies": {
        "@types/node": "^20.11.0",
        "ts-node-dev": "^2.0.0",
        "typescript": "^5.3.3"
    }
}